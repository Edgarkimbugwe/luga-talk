from django.apps import AppConfig

# Configuration class for the 'luga' app
class LugaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'  # Specify the default auto field type
    name = 'luga'  # Define the name of the app
